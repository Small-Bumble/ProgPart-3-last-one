@model IEnumerable<ContractMonthlyClaimSystem.Models.User>

<h2>Users</h2>
<a href="@Url.Action("Create", "User")" class="btn btn-primary">Create New User</a>

<table class="table mt-3">
    <thead>
        <tr>
            <th>Name</th>
            <th>Email</th>
            <th>Role</th>
            <th>Actions</th>
        </tr>
    </thead>
    <a href="c:\users\goodn\downloads\prog6212-part-3-st10268489\contractmonthlyclaimsystem\contractmonthlyclaimsystem\views\user\index.cshtml">c:\users\goodn\downloads\prog6212-part-3-st10268489\contractmonthlyclaimsystem\contractmonthlyclaimsystem\views\user\index.cshtml</a>
    <tbody>
        @foreach (var user in Model)
        {
            <tr>
                <td>@user.FullName</td>
                <td>@user.Email</td>
                <td>@user.Role</td>
                <td>
                    <a href="@Url.Action("Edit", "User", new { id = user.Id })" class="btn btn-warning btn-sm">Edit</a> |
                    <a href="@Url.Action("Delete", "User", new { id = user.Id })" class="btn btn-danger btn-sm">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
